<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
</head>
<body>
    <h1>posts page</h1>
    <a href="/write">글쓰기</a>
    <p>hello <%= user %></p>
    <% posts.forEach((post)=>{ %>
        <div>
            <h3><%= post.title %></h3>
            <p><%= post.content %></p>
            <p><%= post.writer %></p>

            <a href="posts/update?post=<%= post._id %>&&user=<%=post.writer %>">수정</a>
            <form action="posts/delete/<%= post._id %>?_method=DELETE" method="POST">
                <button type="submit">삭제</button>
            </form>
        </div>
    <% })%>
</body>
</html>